name: Legacy Top Plays readability patch retired

# The readability cleanup has already been incorporated into pages/6_Top_Plays.py.
# Keep the historical workflow manual-only so unrelated main pushes cannot retry
# an obsolete source transformation.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Top Plays readability patch is already applied; no action required."
