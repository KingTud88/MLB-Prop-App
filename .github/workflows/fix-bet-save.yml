name: Legacy bet save patch retired

# The manual bet-save persistence fix has already been incorporated into the
# application. Keep this historical workflow inert so unrelated pushes to main
# cannot fail while searching for the old source block.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy bet-save patch is already applied; no action required."
